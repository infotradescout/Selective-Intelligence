name: Did Selective Intelligence work?
description: Tell us whether the outcome worked, partly worked, or was wrong.
title: "[Feedback]: "
labels:
  - feedback
body:
  - type: markdown
    attributes:
      value: |
        Thank you. Share only what is safe to make public. Do not include secrets, private prompts, personal data, or proprietary code.

  - type: dropdown
    id: outcome
    attributes:
      label: Outcome
      description: Which answer best matches the result?
      options:
        - Worked
        - Partly
        - Wrong
    validations:
      required: true

  - type: dropdown
    id: client
    attributes:
      label: Where did you use it?
      options:
        - ChatGPT
        - Codex
        - Claude Code
        - Cursor
        - GitHub Copilot
        - Gemini CLI
        - Kiro
        - Another Agent Skills client
    validations:
      required: true

  - type: dropdown
    id: discovery
    attributes:
      label: How did you find it?
      options:
        - OpenAI Plugins Directory search
        - Direct public listing link
        - Selective Intelligence website
        - GitHub
        - Another person or AI recommended it
        - Somewhere else
    validations:
      required: true

  - type: dropdown
    id: task
    attributes:
      label: What kind of task did you try?
      options:
        - Recover after a correction
        - Turn a rough idea into complete work
        - Finish an interrupted project
        - Reduce wasted tokens or repeated work
        - Challenge a false completion claim
        - Another task
    validations:
      required: true

  - type: textarea
    id: goal
    attributes:
      label: What were you trying to accomplish?
      description: A short, redacted description is enough.
      placeholder: I wanted the AI to...
    validations:
      required: true

  - type: textarea
    id: result
    attributes:
      label: What happened?
      description: Say what helped, what failed, or what was misunderstood.
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: Version or commit (optional)
      description: Include this only if it is easy to find.
      placeholder: 1.0.5 or a commit link

  - type: checkboxes
    id: privacy
    attributes:
      label: Public information check
      options:
        - label: I removed secrets, private prompts, personal data, and proprietary code.
          required: true
